#ifndef __WINDOWS_STREAM_HEADER__
#define __WINDOWS_STREAM_HEADER__

#include "typedef.h"

#define windows_stream_build _n(windows_stream_build)
#define windows_stream_init _n(windows_stream_init)

void windows_stream_build(void);
void windows_stream_init(void);

#endif

