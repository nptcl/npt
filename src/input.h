#ifndef __INPUTLIST_HEADER__
#define __INPUTLIST_HEADER__

#include "execute.h"
#include "typedef.h"

#define readlist_input _n(readlist_input)
_g int readlist_input(Execute ptr, addr file, addr *ret);

#endif

